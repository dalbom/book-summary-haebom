// chapters/01_chapter_name.js
// ────────────────────────────────────────────────────────────────────────────
// TARGET: ~XXXX 자  (약 N 페이지)        ← scripts/budget.js 출력에서 가져옴
// 시간 범위: ___ → ___                    ← 연대기/일지 형식 작품일 때만
// 핵심 사건: ___                          ← 한 줄 요약. 챕터의 정체성.
// ────────────────────────────────────────────────────────────────────────────
//
// ⚠️  작성 원칙 (재발 방지)
// 1. 이 파일은 한 번에 끝까지 쓴다. 절대 "클라이맥스 먼저 박고 나중에 사이 채우기"
//    같이 비순차로 쓰지 말 것 — 시간 도치 버그의 원인.
// 2. 시간 표지(Sol N, 며칠 후, 다음 날 등)가 있는 작품이면 작성 후 닫기 전에
//    파일 통독해서 표지 순서가 단조 증가하는지 확인.
// 3. TARGET 자수의 ±10% 안에서 마무리. 넘치면 Step 7 페이지 튜닝에서 압축,
//    부족하면 다른 챕터 빌리지 말고 이 챕터 안에서 확장.
// 4. 작성 전 books/{작품명}/facts.md를 먼저 읽는다. 챕터 본문은 facts.md의
//    해당 챕터 단락을 충실하게 풀어낸 결과여야 한다 (Step 5).
// ────────────────────────────────────────────────────────────────────────────

const { H1, P, Dialog, Sep, Img } = require('../helpers');

module.exports = [
  // 챕터 시작에 삽화가 있으면 H1 앞에 Img() 한 줄. 없으면 생략.
  // Img('마션_삽화_1.png'),

  H1('1. 첫 번째 장 제목'),

  P('본문 첫 문단.', { firstLine: true }),
  P('본문 두 번째 문단.', { firstLine: true }),

  // 시나리오 형식 — 화자 라벨이 필요할 때 Dialog() 사용.
  Dialog('어린 왕자', '안녕, 아저씨!'),

  // 서술이 화자를 이미 머금고 있을 때는 Dialog() 대신 P()로 한 문단.
  // 라벨 + 서술이 같은 화자를 두 번 부르는 일을 막는다.
  P('"오늘은 어디 가세요?" 어린 왕자가 호기심 어린 얼굴로 물었어요.', { firstLine: true }),

  // 챕터 끝에는 Sep()을 붙이지 않는다 — build.js에서 챕터 사이 Sep을 자동 삽입.
];
